
/*
 * Module dependencies.
 */

import { units } from 'src/ui/styles/dimensions';
import styled from 'styled-components';

/*
 * Export `DialogActions` component.
 */

export const DialogActions = styled.div`
  text-align: right;
  margin-top: ${units(2)}px;
`;
